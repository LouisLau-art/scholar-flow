# =========================
# ScholarFlow Backend ENV 模板
# =========================
#
# 用途：
# - 本地开发：复制为 `backend/.env`（不要提交到 git）
# - Render：把同名变量填到 Render Environment Variables
#

# Supabase（必填）
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# CORS（必填：Vercel 域名）
FRONTEND_ORIGIN=http://localhost:3000

# 环境（生产务必不是 dev）
GO_ENV=dev

# Sentry（UAT 监控）
# - DSN 为空视为禁用（不阻塞启动）
# - traces sample rate: UAT 可设 1.0；正式生产可按需调低
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0
SENTRY_ENABLED=1

# MVP 开关（提速默认）
PLAGIARISM_CHECK_ENABLED=0
PRODUCTION_GATE_ENABLED=0
PLAGIARISM_SIMILARITY_THRESHOLD=0.3
PLAGIARISM_POLL_MAX_ATTEMPTS=5
PLAGIARISM_POLL_INTERVAL_SEC=3
PLAGIARISM_SUBMIT_DELAY_SEC=0.2

# AI 推荐（本地 CPU + 缓存）
MATCHMAKING_WARMUP=1
MATCHMAKING_LOCAL_FILES_ONLY=0

# 邮件/DOI 等（MVP 可留空或 mock）
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
CROSSREF_API_KEY=mock-crossref-key
CROSSREF_DEPOSITOR_EMAIL=
CROSSREF_DEPOSITOR_PASSWORD=
CROSSREF_DOI_PREFIX=10.12345
CROSSREF_API_URL=https://test.crossref.org/servlet/deposit

# Production Email (Resend)
RESEND_API_KEY=

# 远程大模型（MVP 默认不用；留空）
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL_ID=

# Email sender（如使用 Resend/SMTP）
EMAIL_SENDER=ScholarFlow <no-reply@send.yourdomain.com>
