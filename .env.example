# =========================
# ScholarFlow (Root) ENV 模板
# =========================
#
# 用途：
# - 本地开发：复制为 `.env`（不要提交到 git）
# - 部署：把同名变量填到 Vercel / Render 的环境变量里
#
# 安全：
# - 任何密钥（尤其是 SUPABASE_SERVICE_ROLE_KEY）严禁提交到仓库
#

# Supabase（云端项目）
SUPABASE_URL=
SUPABASE_ANON_KEY=

# 仅后端需要（绝不下发到前端）
SUPABASE_SERVICE_ROLE_KEY=

# 环境
GO_ENV=dev

# 部署域名（生产环境建议填写）
FRONTEND_ORIGIN=http://localhost:3000
BACKEND_ORIGIN=http://127.0.0.1:8000

# MVP 开关（提速默认）
PLAGIARISM_CHECK_ENABLED=0
PRODUCTION_GATE_ENABLED=0

# AI 推荐（本地 CPU + 缓存）
MATCHMAKING_WARMUP=1
MATCHMAKING_LOCAL_FILES_ONLY=0
HF_ENDPOINT=https://hf-mirror.com

# Sentry（Feature 027，全栈监控）
# 前端 DSN（会暴露到浏览器，属于“可公开配置”）
NEXT_PUBLIC_SENTRY_DSN=
# 后端 DSN（仅后端使用）
SENTRY_DSN=
# sourcemaps 上传（构建时使用，属于敏感信息，仅放平台 Secret）
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0
SENTRY_ENABLED=1
